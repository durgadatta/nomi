# ══════════════════════════════════════════════════════════════════════
#   Nomi by Example — A Guided Tour
#
#   Each section introduces one language feature with simple examples.
#   Run with:  nomi samples/demo.nomi
# ══════════════════════════════════════════════════════════════════════

# ── 1. Functions ───────────────────────────────────────────────────
# Traditional function definition
func greet(name):
    return "Hello, " + name

# Arrow function — single-param (no parens) or multi-param
square = x => x * x
add = (a, b) => a + b

# Lightweight equation — f(a, b) = expr
pi() = 3.14

# Underscore hole-filling lambdas (Scala-style)
double = _ * 2
upcase = _.upper()

# Piecewise definitions (Haskell-style)
# Contiguous equations with the same name merge into one function.
fact(1) = 1
fact(n) = fact(n - 1) * n

# Piecewise with guards — add "when" conditions to equation clauses
sign(n) when n > 0 = 1
sign(n) when n < 0 = -1
sign(n) = 0

# Single-arg equation without parens — omit () for a single variable param
sq x = x * x

# Positional hole lambdas — $1, $2 refer to positional params (Swift-style)
inc = $1 + 1
fmt = $1 + $2 + $3

# Named hole lambdas — $name uses the identifier as the parameter name
greet = "Hello, " + $name
fullname = $first + " " + $last

# Where clause — local bindings
area = pi * r * r where:
    pi = 3.14159
    r = 5

twice = x * 2 where x = 5    # inline form


# ── 2. Control Flow ─────────────────────────────────────────────────
# unless — Ruby-style inverted if
x = 5
unless x > 10:
    label = "not big"         # executes when condition is False

# Implicit multiplication — 2x means 2*x, 2(x+y) means 2*(x+y)
n = 5
twice_n = 2n                   # 10
poly = 2(n + 1)                # 12


# ── 3. Null Handling ────────────────────────────────────────────────
# Null coalescing: use a default when value is None
name = None ?? "anonymous"

# Optional chaining: safe navigation
d1 = {"key": "found"}
d2 = None
v1 = d1?.get("key")     # "found"
v2 = d2?.get("key")     # None


# ── 4. Collections & Data ───────────────────────────────────────────
# Ranges: 1..5 (inclusive), 1..<5 (exclusive)
one_to_five = list(1..5)
one_to_four = list(1..<5)

# Pipeline: thread a value through functions
total = 1..10 |> list |> sum    # 55


# ── Results ─────────────────────────────────────────────────────────
print("1. greet(Alice) = " + greet("Alice"))
print("1. square(5) = " + str(square(5)))
print("1. add(3,4) = " + str(add(3, 4)))
print("1. pi() = " + str(pi()))
print("1. double(21) = " + str(double(21)))
print("1. upcase(hi) = " + upcase("hi"))
print("1. fact(5) = " + str(fact(5)))
print("1. sign(10) = " + str(sign(10)))
print("1. sign(-5) = " + str(sign(-5)))
print("1. sign(0)  = " + str(sign(0)))
print("1. sq(5) = " + str(sq(5)))
print("1. inc(7) = " + str(inc(7)))
print("1. fmt(a,b,c) = " + fmt("a", "b", "c"))
print("1. greet(world) = " + greet("world"))
print("1. fullname(Ada, Lovelace) = " + fullname("Ada", "Lovelace"))
print("1. area = " + str(area))

print("2. label = " + label)
print("2. 2n where n=5 = " + str(twice_n))
print("2. 2(n+1) where n=5 = " + str(poly))

print("3. None ?? anonymous = " + name)
print("3. d1?.get(key) = " + v1)
print("3. d2?.get(key) = " + str(v2))

print("4. 1..5 = " + str(one_to_five))
print("4. 1..<5 = " + str(one_to_four))
print("4. 1..10 |> sum = " + str(total))
